<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Birthday Card for Thousif 🎂</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #ff758c, #ff7eb3, #8e44ad);
      overflow: hidden;
      color: white;
    }

    .card {
      width: 100%;
      max-width: 500px;
      padding: 35px 25px;
      text-align: center;
      border-radius: 25px;
      background: rgba(255, 255, 255, 0.18);
      backdrop-filter: blur(15px);
      box-shadow: 0 15px 40px rgba(0,0,0,0.3);
      animation: showCard 1.2s ease;
    }

    .cake {
      font-size: 75px;
      animation: bounce 1.5s infinite;
    }

    h1 {
      font-size: clamp(2.2rem, 10vw, 4rem);
      margin: 10px 0;
      text-shadow: 0 0 20px white;
    }

    h2 {
      font-size: clamp(1.4rem, 6vw, 2rem);
      margin-bottom: 20px;
    }

    .message {
      font-size: 18px;
      line-height: 1.7;
      margin-bottom: 20px;
    }

    .from {
      font-size: 17px;
      font-weight: bold;
      margin-top: 15px;
    }

    button {
      margin-top: 25px;
      padding: 14px 25px;
      border: none;
      border-radius: 30px;
      background: white;
      color: #e84393;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      transform: scale(1.08);
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-12px);
      }
    }

    @keyframes showCard {
      from {
        opacity: 0;
        transform: scale(0.6);
      }

      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    @media (max-width: 480px) {
      .card {
        padding: 30px 18px;
      }

      .cake {
        font-size: 60px;
      }

      .message {
        font-size: 16px;
      }
    }
  </style>
</head>

<body>

  <div class="card">

    <div class="cake">🎂</div>

    <h1>Happy Birthday!</h1>

    <h2>Dear Thousif 💖</h2>

    <p class="message">
      Wishing you a very Happy Birthday! 🎉
      May your day be filled with happiness, laughter,
      amazing memories and lots of surprises. ✨
      <br><br>
      Keep smiling, keep shining and have an incredible year ahead! 🥳
    </p>

    <p class="from">
      With lots of wishes,<br>
      💕 Abhinaya
    </p>

    <button onclick="celebrate()">
      🎊 Celebrate! 🎊
    </button>

  </div>

  <script>

    function celebrate() {

      const colors = [
        "#ff4757",
        "#ffd32a",
        "#05c46b",
        "#18dcff",
        "#9c88ff",
        "#ff6bcb",
        "#ffffff"
      ];

      // Create confetti
      for (let i = 0; i < 180; i++) {

        const confetti = document.createElement("div");

        confetti.style.position = "fixed";
        confetti.style.left = Math.random() * 100 + "vw";
        confetti.style.top = "-20px";

        confetti.style.width = "8px";
        confetti.style.height = "14px";

        confetti.style.background =
          colors[Math.floor(Math.random() * colors.length)];

        confetti.style.zIndex = "9999";
        confetti.style.pointerEvents = "none";

        document.body.appendChild(confetti);

        const duration = 2 + Math.random() * 3;

        confetti.animate(
          [
            {
              transform: "translateY(0) rotate(0deg)",
              opacity: 1
            },
            {
              transform:
                "translateY(110vh) rotate(720deg)",
              opacity: 0.8
            }
          ],
          {
            duration: duration * 1000,
            easing: "linear"
          }
        );

        setTimeout(() => {
          confetti.remove();
        }, duration * 1000);
      }

      // Floating hearts
      for (let i = 0; i < 20; i++) {

        const heart = document.createElement("div");

        heart.innerHTML = "💖";

        heart.style.position = "fixed";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.bottom = "-30px";
        heart.style.fontSize = "25px";
        heart.style.zIndex = "9999";

        document.body.appendChild(heart);

        heart.animate(
          [
            {
              transform: "translateY(0)",
              opacity: 1
            },
            {
              transform: "translateY(-110vh)",
              opacity: 0
            }
          ],
          {
            duration: 4000 + Math.random() * 2000,
            easing: "linear"
          }
        );

        setTimeout(() => {
          heart.remove();
        }, 6000);
      }
    }

    // Automatic celebration when the page opens
    window.onload = celebrate;

  </script>

</body>
</html>